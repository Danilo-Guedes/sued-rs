//! Audio via `kira` (M3) — looping dread ambience + triggered scare stings.
//! Compiled only with `--features audio` so the project builds without ALSA headers.
//
// TODO(M3): kira setup, looping ambience, reveal sting. Runs on its own thread.
