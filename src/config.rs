//! Config load/save via `serde` + `serde_json` (M5): intro/decoy lines, theme,
//! language, audio volume. Mirrors the original SueD's configurable fake answers.
//
// TODO(M5): define the serde `Config` structs and load/save helpers.
